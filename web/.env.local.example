# Next.js environment variables — copy to .env.local and fill in values
# NEVER put SERVICE_ROLE_KEY here — that lives only in the Python .env

# Supabase (read-only anon key — safe to expose in frontend)
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_LRhz5t-JzBFyC113GQTVTQ_TcbPOV95
